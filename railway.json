{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && composer install --optimize-autoloader --no-dev && cd ../frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "environments": {
    "production": {
      "build": {
        "buildCommand": "cd backend && composer install --optimize-autoloader --no-dev && cd ../frontend && npm install && npm run build"
      },
      "deploy": {
        "startCommand": "cd backend && php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
      }
    }
  }
}